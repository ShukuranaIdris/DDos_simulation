Simple UDP Packet Sender

A lightweight Python script designed to test network latency and packet transmission across designated ports using UDP sockets.

# Disclaimer
> This tool is intended for educational purposes and authorized network testing only. Do not use this script against targets without explicit permission.

# Requirements
* Python 3.x
* Standard libraries only (`os`, `sys`, `time`, `socket`, `random`)

# Usage
1. Clone the repository:
   ```bash
   git clone https://github.com/ShukuranaIdris/DDos_simulation.git
